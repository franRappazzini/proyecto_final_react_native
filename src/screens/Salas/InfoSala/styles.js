import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontSize: 20,
    textAlign: 'center',
  },
  iconContainer: {
    marginTop: 10,
    alignItems: 'center',
  },
});
