import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  switchContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginVertical: 10,
  },
  textSwtich: {
    fontSize: 16,
  },
  crearSalaContainer: {
    marginVertical: 10,
  },
});
