import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  welcomeText: {
    textAlign: 'center',
    fontSize: 30,
    fontWeight: '600',
    marginBottom: 16,
  },
  inputsContainer: {
    marginBottom: 15,
  },
});
