import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textNoMensajes: {
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center',
    marginTop: 10,
  },
  textCrearChat: {
    fontSize: 16,
    textAlign: 'center',
  },
});
